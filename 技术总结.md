# DeltaForge v6.1 — 三角洲行动 运行环境管理 技术总结

> 文档生成: 2026-07-25
> 仓库: github.com/wzy887011/DeltaForge
> 分支: master

---

## 一、项目当前进展

### 2026-07-25 状态: 稳定可用，待长测验证

**已完成:**

| 模块 | 状态 | 说明 |
|------|------|------|
| 注入方式 | ✅ ptrace 注入 | injector.c 通过 ptrace attach → shellcode dlopen 注入 libforgehook.so |
| 内存补丁 | ✅ 113 处满分 | /proc/pid/mem 写入 tersafe代码67处 + BSS40处 + UE4 6处 |
| libc Hook | ✅ 16 函数 | g_hooks_ready 延迟激活，constructor 150 后立即启用 |
| 网络过滤 | ✅ | getaddrinfo 拦截14域名 + connect 拦截5 IP段 |
| 属性伪装 | ✅ libc层 | __system_property_get hook 返回 Samsung S10 属性 |
| JNI Build 字段 | ✅ | 覆盖 android.os.Build 为 Samsung Galaxy S10 |
| 部署脚本 | ✅ | deploy.sh --no-hijack 一键编译+部署+恢复原版 qimei |

**已知缺陷:**

| 模块 | 状态 | 说明 |
|------|------|------|
| JNI SystemProperties | ❌ 暂禁用 | RegisterNatives 替换整表导致 inject 模式闪退 |
| BPF seccomp | ❌ 暂禁用 | exit_group 拦截与游戏退出流程冲突 |
| GPU 伪装 | ❌ 暂禁用 | 云手机虚拟 GPU 无需伪造，待验证 |
| 文件清理 | ❌ 暂禁用 | 删除环境检测文件被游戏检测为"第三方插件" |
| hijack 模式 | ❌ 已弃用 | so 替换方案与 tersafe 完整性校验无法共存 |
| 特征码扫描 | ⚠️ 框架已就 | kill chain pattern scan 已实现，待填入实际特征码 |

**被封经验:**

- 1 分钟内被封 = 文件清理被检测为"第三方插件" → 已修复（禁用文件清理）
- 当前配置可登录游戏，需长测验证（建议挂机 2-3 小时无账号限制才稳定）

---

## 二、技术架构

### 2.1 总体方案

```
PC (Windows) — 代码编辑
  │ git push
  ▼
GitHub (wzy887011/DeltaForge)
  │ git pull
  ▼
云手机 (Termux) — clang 编译 + root 部署
  │ forge -l
  ▼
三角洲行动游戏进程
  ├── ptrace 注入 libforgehook.so
  ├── /proc/pid/mem 写入 113 处内存补丁
  ├── libc 函数 hook (open/fopen/stat/connect/getaddrinfo 等 16 个)
  ├── __system_property_get hook 属性伪装
  ├── JNI JNI_OnLoad → jni_overwrite_build_fields (Build 字段覆盖)
  └── 后台守护: 每 200ms 检查补丁完整性，防止 tersafe 恢复
```

### 2.2 五层防护模型

| 层级 | 技术 | 功能 |
|------|------|------|
| L1 注入层 | ptrace + dlopen | 将 libforgehook.so 注入游戏进程 |
| L2 内存层 | /proc/pid/mem 写入 | 113处指令补丁瘫痪 tersafe 检测逻辑 |
| L3 libc层 | 符号拦截 | 文件/属性/信号/网络/链接器 16函数全覆盖 |
| L4 JNI层 | JNI_OnLoad | 覆盖 android.os.Build 30个字段为 Samsung S10 |
| L5 守护层 | double-fork daemon | 每200ms检查补丁完整性，防止 tersafe 恢复 |

### 2.3 目录结构

```
DeltaForge/
├── cloud-agent/
│   ├── native/
│   │   ├── forge.c              # 主控: 属性伪装/文件清理/内存补丁/TCP server
│   │   ├── libforgehook.c       # 注入库: libc拦截/JNI/网络/代码补丁
│   │   ├── injector.c           # ptrace 注入器
│   │   ├── forge_monitor.c      # inotify 行为监控
│   │   └── touch_injector.c     # /dev/uinput 触摸伪装
│   ├── deploy.sh                # 一键编译+部署
│   ├── check.sh                 # 诊断脚本
│   └── df-hijack-root.sh        # hijack 安装 (已弃用)
└── 项目状态文档.md              # 全模块审计报告
```

### 2.4 关键编译参数

```bash
# Termux 环境
CC=clang
forge:         clang -pie -Os -Wall forge.c -o forge
libforgehook:  clang -shared -fPIC -Os -Wall libforgehook.c -o libforgehook.so -ldl
forge_monitor: clang -pie -Os -Wall forge_monitor.c -o forge_monitor
injector:      clang -pie -Os -Wall injector.c -o injector -ldl
touch:         clang -pie -Os -Wall touch_injector.c -o touch_injector
```

---

## 三、云端仓库

### 地址

```
git@github.com:wzy887011/DeltaForge.git
https://github.com/wzy887011/DeltaForge
```

### 上传流程 (PC → GitHub)

```bash
cd "D:\下载\cc-chain-forge (3)\DeltaForge-repo"
git add -A
git commit -m "描述改动"
git push origin master
```

### 云手机拉取 + 部署

```bash
# 一键更新并部署
cd ~/DeltaForge && git pull && sh cloud-agent/deploy.sh --no-hijack

# 杀掉旧进程，启动游戏
su -c 'pkill -9 -f forge; am force-stop com.tencent.tmgp.dfm'
sleep 2
su -c '/data/local/tmp/forge -l'
```

### 诊断命令

```bash
# 完整诊断
su -c 'sh /data/local/tmp/check.sh'

# 查看 hook 激活状态
su -c 'grep "hooks.*activated" /data/local/tmp/forge_hook.log'

# 查看补丁稳定性
su -c 'grep "patch reverted" /data/local/tmp/forge.log | tail -10'

# 查看游戏进程
su -c 'pidof com.tencent.tmgp.dfm'
```

---

## 四、云手机配置

| 项目 | 配置 |
|------|------|
| 型号伪装 | Samsung SM-G9730 (beyond1q) |
| SoC 伪装 | Snapdragon 8+ Gen1 (SM8475 Kailua) |
| CPU 伪装 | 1×Cortex-X2(0xd48) + 3×A710(0xd47) + 4×A510(0xd46) |
| GPU | 云手机虚拟 GPU (实际型号未知) |
| Android | 11 (SDK 30) |
| 指纹 | samsung/beyond1qltezc/beyond1q:11/.../G9730ZCS6FULZ |
| Root | 已 root, su 可用 (uid=0) |
| Magisk | 未安装 (无 resetprop，属性伪装靠 libc hook 兜底) |
| Termux | 已安装，提供 clang 编译链 |
| 部署目录 | /data/local/tmp/ |
| 游戏包名 | com.tencent.tmgp.dfm |

---

## 五、调试经验总结

### 5.1 闪退排查全链路

| 问题 | 根因 | 修复 |
|------|------|------|
| BPF 信号拦截导致闪退 | tgkill/tkill 拦截破坏 ART 线程管理 | 精简为 exit_group only → 最终禁用 |
| GPU hook 导致黑屏 | 假 Adreno 730 字符串不匹配云手机 GPU | 禁用 GPU hook |
| libc hook 过早介入 | 游戏初始化时 hook 打断关键路径 | g_hooks_ready 延迟激活 |
| hijack 模式一直闪退 | so 替换导致 tersafe 完整性校验失败 + JNI_OnLoad 丢失 | 弃用 hijack，改用 ptrace inject |
| deploy.sh --no-hijack 实际在 hijack | heredoc 变量未传递，旧 so 残留 | 修复 heredoc 变量传递 + 自动恢复原版 qimei |
| inject 模式 texts file busy | 旧 forge 进程活着导致新二进制写不进去 | deploy.sh 加 pkill -9 + sleep 3 |
| 1分钟被封 | 文件清理被游戏检测为"第三方插件" | 禁用文件清理 |
| 反复被封 | hooks 未激活导致裸奔 | constructor 150 立即激活 hooks |

### 5.2 血的教训

1. **永远不要做 so 文件替换 (hijack)** — 与 tersafe 完整性校验无法共存，2026 年没有任何人用这种方案
2. **deploy.sh 的 heredoc 变量传递是坑** — 单引号块内不展开 shell 变量
3. **部署前先确保 forge 进程死透** — `pkill -9 + sleep 3` 不能省略
4. **测试时每个账号只用一次** — 被封过的号可能已被标记
5. **文件清理太激进反而暴露自己** — 游戏会检测自己的文件被删
