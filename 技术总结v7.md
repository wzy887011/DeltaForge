# DeltaForge v7.0 — 使用报告与技术讲解

> 文档路径: `DeltaForge/技术总结v7.md`
> 适用版本: v7.0 (commit fff5a5a)
> 平台: Android ARM64 / 云手机 / Termux

---

## 目录

1. [项目概述](#1-项目概述)
2. [架构总览](#2-架构总览)
3. [核心模块技术讲解](#3-核心模块技术讲解)
4. [v7.0 改进详解](#4-v70-改进详解)
5. [部署使用指南](#5-部署使用指南)
6. [常见问题排查](#6-常见问题排查)

---

## 1. 项目概述

DeltaForge 是一套针对 Android 云手机环境的**运行时环境管理工具集**，
目标是使游戏（`com.tencent.tmgp.dfm`）在 root 过的云手机上能够
绕过其内置的 `libtersafe.so` 环境检测模块，稳定运行。

### 技术挑战

| 检测类型 | libtersafe 行为 | 应对方案 |
|----------|-----------------|----------|
| Root 检测 | 读 /system/bin/su, /sbin/magisk | HIDDEN 数组拦截 |
| 虚拟化检测 | 读 /sys/bus/virtio, /proc/cpuinfo | OVERRIDE_FILES 伪造 |
| 设备 fingerprint | `__system_property_get` 查询 ro.build.* | hook 属性查询 |
| 内存完整性 | 自检代码段后 tgkill/exit_group 杀进程 | 代码段 patch + hook 拦截 |
| 进程状态 | 读 /proc/self/status TracerPid 字段 | 动态生成含真实 PID 的伪造内容 |

---

## 2. 架构总览

```
┌─────────────────────────────────────────────────────┐
│                  Android 云手机                       │
│                                                     │
│  ┌──────────────┐    ptrace注入    ┌──────────────┐ │
│  │   forge.c    │ ──────────────► │libforgehook.c│ │
│  │  (守护进程)   │                 │ (进程内拦截)  │ │
│  │              │ ◄── TCP 9510 ── │              │ │
│  │  - 代码 patch │                 │ - libc hook  │ │
│  │  - BSS 清零  │                 │ - 文件伪造   │ │
│  │  - 属性适配  │                 │ - 属性 hook  │ │
│  └──────────────┘                 └──────────────┘ │
│         ▲                                           │
│         │ adb forward 9510                          │
└─────────│───────────────────────────────────────────┘
          │
┌─────────┴───────────────────────────────────────────┐
│              PC 端 (Python)                          │
│                                                     │
│  forge_controller.py  ──── SipHash MAC ────►  forge │
│  bot_runner.py        崩溃恢复 → 重打 patch          │
│  forge_monitor_v7.py  root 特权读真实网络连接         │
└─────────────────────────────────────────────────────┘
```

### 注入方式对比

| 模式 | 原理 | 优点 | 缺点 |
|------|------|------|------|
| **hijack 模式** | 将 libforgehook.so 替换为游戏目录下的 libtdmqimei.so | 无需 root ptrace | constructor 链在 linker 阶段运行，时机早，易死锁 |
| **inject 模式** (推荐) | ptrace 在游戏完全初始化后注入 | hook 激活时机安全 | 需要 root ptrace 权限 |

---

## 3. 核心模块技术讲解

### 3.1 libforgehook.c — constructor 链执行顺序

Android linker 在加载 so 时按 constructor 优先级从小到大执行，
v7.0 的执行顺序如下：

```
constructor(47)  _resolve_qimei_path
  └─ 仅解析 libtdmqimei_real.so 的磁盘路径，缓存到 g_real_qimei_path
     不调 dlopen（避免 linker 持锁期间重入 g_dl_mutex → 死锁）

constructor(48)  _probe_loaded
  └─ 用裸 syscall 写一行到 forge_hook.log，确认 so 已执行

constructor(49)  [已禁用] 原 seccomp 安装（v6.0 因拦截 ART 信号导致崩溃）

constructor(50)  _hide_self_from_maps
  └─ madvise(MADV_DONTDUMP) 标记自身内存，core dump 不含注入段

constructor(101) _hide_from_linker_list
  └─ 从 r_debug.r_map 链表摘除 libforgehook + libtdmqimei_real，
     防止 dl_iterate_phdr 遍历发现注入

constructor(120) [已禁用] GPU 字符串 hook（云手机 GPU 与伪造值不匹配导致黑屏）

constructor(150) _adjust_code
  └─ 启动 detached 后台线程 _adjust_code_thread
     ├─ pthread_once 执行 chainload（dlopen 原版 qimei，linker 锁已释放）
     ├─ 轮询最多 60s 等待 libtersafe.so 加载
     ├─ 应用检测链 6 节点代码 patch
     └─ [v7.0] patch 成功(ok>=4)后才设 g_hooks_ready=1，激活所有 libc hook
```

**关键设计: g_hooks_ready 延迟激活**

```
hijack 模式加载时序:
  t=0ms   so 加载，constructor 链开始执行
  t=1ms   constructor(150) 启动后台线程后返回
  t=2ms   游戏继续初始化（ART/UE4/tersafe 依次加载）
  t=600ms tersafe.so 加载完毕
  t=601ms 后台线程检测到 tersafe，执行代码 patch
  t=650ms patch 完成，设 g_hooks_ready=1
  t=651ms 所有 libc hook 生效 ← 此时 tersafe 代码已被 patch，安全

原版 v6.0 错误时序:
  t=1ms   constructor(150) 末尾立即设 g_hooks_ready=1
  t=2ms   所有 hook 激活，但 tersafe 尚未 patch
  t=600ms tersafe 加载，运行检测代码 → 发现 root → tgkill → 游戏退出
```

### 3.2 forge.c — 内存 patch 机制

forge.c 通过 `/proc/<pid>/mem` 向目标进程写入 ARM64 指令：

**ARM64 指令编码说明**

| 指令值 | 助记符 | 含义 | 使用场景 |
|--------|--------|------|----------|
| `0xD2800000` | `MOV X0, #0` | X0 = 0 | 检测入口返回"安全" |
| `0xD65F03C0` | `RET` | 函数返回 | 跳过检测函数体 |
| `0xD61F03C0` | `BR X30` | 间接返回 | 检测调度层绕过 |
| `0x2A1F03FF` | `MOV W0, #0xFF` | W0 = 255 | 模拟"检测通过"返回值 |
| `0x1400000X` | `B #+offset` | 无条件跳转 | 跳过检测逻辑块 |
| `0x38400XXX` | `LDRB Wx,[Xsp,#N]` | 从栈读 | 改为读栈(值趋于0)替代读文件 |

**检测链 6 节点 patch 顺序**

```
libtersafe.so 检测入口 (offset 0x419FDC)
  MOV X0,#0          ← 入口直接置返回值为0("无问题")
  RET                ← 立即返回，不执行任何检测

dispatch (0x2E7810) → router (0x2F29D0) → wrapper (0x320D78)
  全部改为 RET        ← 即使入口 patch 失效，调度链也不会传递到 tgkill

tgkill 调用点 (0x3233B8)
  RET                ← 最后一道防线，tgkill 调用点直接返回
```

### 3.3 libforgehook.c — 文件系统伪造

所有文件访问通过 open/openat/fopen hook 拦截，匹配后返回 memfd：

```c
// 匹配路径 → 生成内存文件 → 返回 fd
int open(const char *path, int flags, ...) {
    if (hidden(path))         → return ENOENT   // 完全隐藏
    if (null_redir(path))     → return /dev/null // 重定向空
    if (is maps/smaps path)   → make_filtered_maps_fd()  // 动态过滤
    if (is /proc/*/status)    → ensure_proc_status()     // 动态 PID
    if (match OVERRIDE_FILES) → override_fd(data, len)   // 静态伪造
}
```

**覆盖的 /proc 路径（v7.0 新增）**

| 路径 | 伪造内容 | 目的 |
|------|----------|------|
| `/proc/self/status` | 动态生成，含真实 PID，TracerPid=0 | 隐藏 ptrace/调试状态 |
| `/proc/self/maps` | 过滤 libforgehook/frida/substrate 行 | 隐藏注入 |
| `/proc/self/smaps` | 同 maps 过滤（按段块过滤）| 隐藏注入内存详情 |
| `/proc/self/wchan` | `do_epoll_wait` | 隐藏 seccomp/ptrace 状态 |
| `/proc/self/syscall` | read() 的寄存器状态 | 隐藏当前系统调用 |
| `/proc/self/attr/current` | `u:r:untrusted_app:s0:...` | 伪造 SELinux 上下文 |
| `/proc/self/environ` | 清空（隐藏 LD_PRELOAD 等）| 隐藏注入环境变量 |
| `/proc/net/tcp{,6}` | 空连接表 | 隐藏调试端口 |
| `/sys/bus/virtio/*` | ENOENT | 隐藏虚拟化特征 |

### 3.4 SipHash-2-4 TCP 认证（v7.0 新增）

防止同机其他进程伪造 forge 控制命令（stop/patch 等）。

**通信协议**

```
客户端发送:
  AUTH:<16hex_nonce>:<16hex_mac>\n
  <command>\n

示例:
  AUTH:a3f4b2c1d5e6f708:9e8f7a6b5c4d3e2f\n
  launch\n

forge.c 验证:
  expected_mac = SipHash24(session_key, nonce || command)
  if expected_mac != client_mac → 拒绝连接
```

**session_key 生命周期**

```
forge daemon 启动
  → 读 /data/local/tmp/.forge_key (chmod 600)
  → 不存在则从 /dev/urandom 生成 16 字节随机 key
  → 写入 .forge_key

forge_controller.py
  → adb shell su -c 'base64 .forge_key' 拉取 key
  → 缓存到本地 /tmp/.forge_key_cache (chmod 600)
  → 每次发命令前 secrets.token_bytes(8) 生成 nonce + 计算 MAC
```

---

## 4. v7.0 改进详解

### 4.1 [P0-1] g_hooks_ready 时序修复

**问题根源**: v6.0 在 constructor(150) 末尾立即 `g_hooks_ready=1`，
此时后台线程还未找到 libtersafe.so，代码 patch 尚未执行。

**修复代码位置**: `libforgehook.c` → `_adjust_code_thread`

```c
// 修复后: patch 完成后才激活
if (ok >= (int)KILL_CHAIN_N - 2) {
    __atomic_store_n(&g_hooks_ready, 1, __ATOMIC_RELEASE);
    hook_log("[hooks] v7 activated after patch success\n");
}
// constructor(150) 末尾: 移除了提前激活的代码
```

### 4.2 [P0-2] maps/smaps 动态缓冲修复

**问题根源**: v6.0 `char buf[65536]` 静态 64KB 缓冲，
大型游戏进程（数百个 so）maps 文件超过 64KB，截断后注入痕迹残留。

**修复逻辑**: `libforgehook.c` → `make_filtered_maps_fd`

```c
// 修复后: 动态扩容
size_t cap = 262144; // 从 256KB 开始
while (cap <= 2 * 1024 * 1024) {
    raw = mmap(NULL, cap+1, PROT_READ|PROT_WRITE, MAP_PRIVATE|MAP_ANONYMOUS, -1, 0);
    total = 0;
    while ((n = read(rfd, raw+total, cap-total)) > 0) total += n;
    if ((size_t)total < cap) break; // 读完了
    munmap(raw, cap+1); cap *= 2;   // 不够大，翻倍
}
```

### 4.3 [P1-1] constructor(47) dlopen 死锁修复

**问题根源**: Android 10+ linker 的 `g_dl_mutex` 不可重入。
在 constructor 执行期间（linker 持锁），再次调用 `dlopen` 会
尝试获取同一把锁，造成死锁。

**修复方案**: constructor(47) 只缓存路径；
实际 `dlopen` 在 `_adjust_code_thread` 中通过
`pthread_once(&g_chainload_once, _do_chainload)` 执行，
此时 linker 已完成所有工作，锁已释放。

### 4.4 [Patch-A] tgkill 全面拦截

**问题根源**: libtersafe 可能发 SIGABRT(6) 等非 9/15 信号自杀，
v6.0 的 `if (sig == 9 || sig == 15) return 0` 无法拦截。

**修复方案**: 检查调用方 return address 是否在 libtersafe 代码段内：

```c
int tgkill(pid_t tgid, pid_t tid, int sig) {
    // 检查 return address 是否在 libtersafe 代码段
    if (g_ts_text_start) {
        uintptr_t ra = (uintptr_t)__builtin_return_address(0);
        if (ra >= g_ts_text_start && ra < g_ts_text_end) {
            if (sig != 0 && sig != SIGCHLD && sig != SIGPIPE)
                return 0; // 来自 tersafe 的终止信号，全部丢弃
        }
    }
    if (sig == 9 || sig == 15) return 0; // 兜底
    return _tgkill(tgid, tid, sig);
}
```

### 4.5 [Fix] inject_hook execv 安全修复

**问题根源**: v6.0 用字符串拼接 + `system()` 调用 injector：

```c
// 危险: hook_path 含单引号时 shell 解析错误
snprintf(cmd, ..., "/data/local/tmp/injector %d '%s'", pid, hook_path);
system(cmd);
```

**修复方案**: fork + execv 直接传参数数组：

```c
char *const argv[] = { "/data/local/tmp/injector", pid_str, hook_path, NULL };
pid_t child = fork();
if (child == 0) { execv(argv[0], argv); _exit(127); }
// 带 10s 超时等待子进程
```

---

## 5. 部署使用指南

### 5.1 环境要求

```
设备: Android ARM64 root 过的云手机
工具: Termux (含 clang), adb, Python 3.8+
权限: root shell, adb 连接
```

### 5.2 编译

```bash
# 在 Termux 内执行
cd /path/to/DeltaForge/cloud-agent
sh deploy.sh --no-hijack    # 推荐: 不使用 hijack 模式
# 或
sh deploy.sh --dry-run      # 只编译验证，不部署
```

编译产物：
```
/data/local/tmp/forge          # 主控守护进程
/data/local/tmp/libforgehook.so # 进程内拦截库
/data/local/tmp/injector        # ptrace 注入器
/data/local/tmp/touch_injector  # 触摸注入
/data/local/tmp/forge_monitor   # 行为监控
```

### 5.3 运行流程

**方案一: 命令行直接运行（最简单）**

```bash
# 在手机 root shell 内
su -c '/data/local/tmp/forge -l'
# -l: prepare + 启动游戏 + 注入 + patch，全自动
```

**方案二: 通过 PC 端 Python 控制（推荐）**

```bash
# PC 端
cd DeltaForge/runner

# 1. 完整启动（清理+属性适配+启动游戏+patch）
python forge_controller.py full --serial <adb_serial>

# 2. 仅 patch（游戏已在运行）
python forge_controller.py patch

# 3. 查看状态
python forge_controller.py status
```

**方案三: 配合 bot_runner 自动化**

```python
# runner/bot_runner.py 示例
from forge_controller import ForgeController
from bot_runner import BotRunner

ctrl = ForgeController()
ctrl.connect()

bot = BotRunner()
bot.set_forge_controller(ctrl)  # 注入 ctrl，崩溃后自动重打 patch
bot.run()
```

### 5.4 forge TCP 控制命令

守护进程 `forge -d` 监听 `127.0.0.1:9510`，支持以下命令：

| 命令 | 功能 | 超时 |
|------|------|------|
| `ping` | 心跳检测，返回版本号 | 5s |
| `prepare` | 清理遥测文件+适配属性+关闭游戏 | 120s |
| `launch` | prepare + 启动游戏 + 注入 + patch | 180s |
| `patch` | 仅对运行中的游戏 patch | 60s |
| `stop` | force-stop 游戏 | 10s |
| `status` | 游戏 PID / 是否运行 | 10s |
| `clean` | 删除遥测文件 | 30s |
| `adapt` | 修改系统属性（resetprop）| 30s |

v7.0 所有命令需要 `AUTH:<nonce>:<mac>\n` 前缀（`forge_controller.py` 自动处理）。

### 5.5 日志文件说明

| 文件 | 内容 | 用于 |
|------|------|------|
| `/data/local/tmp/forge.log` | forge 守护进程运行日志 | 排查启动/patch 问题 |
| `/data/local/tmp/forge_hook.log` | libforgehook constructor 链 + patch 结果 | 排查注入/hook 问题 |
| `/data/local/tmp/forge_repair.log` | 守护进程检测到的 patch 恢复事件 | 分析 tersafe 反制行为 |
| `/data/local/tmp/forge_monitor.log` | 文件访问/网络连接监控 | 发现未覆盖的检测路径 |
| `/data/local/tmp/forge_bss_map.json` | 动态扫描到的 BSS 检测变量偏移缓存 | 跨版本复用 |
| `/data/local/tmp/detect_now.log` | logcat 过滤的反作弊相关日志 | 查看 tersafe 输出 |

---

## 6. 常见问题排查

### Q1: 游戏在 constructor 链后闪退

**诊断**:
```bash
adb shell su -c 'cat /data/local/tmp/forge_hook.log'
```
查看最后一条 `[CTOR]` 日志，定位到哪个 constructor 未完成。

**常见原因及处理**:
| 症状 | 原因 | 处理 |
|------|------|------|
| constructor(47) 后无日志 | dlopen 死锁 | 已在 v7.0 修复 |
| hooks activated 但游戏仍崩溃 | patch 失败后 tersafe 检测通过 | 查看 forge_hook.log 中 `patch fail` 数量 |
| 无 forge_hook.log | so 未加载 | 检查 SELinux: `adb shell dmesg | grep avc` |

### Q2: tgkill 拦截无效

**诊断**:
```bash
adb shell su -c 'grep -i "kill\|exit_group" /data/local/tmp/forge_hook.log | tail -20'
```

**可能原因**:
- libtersafe 使用直接 SVC 指令（绕过 libc hook）
- 需要重新检查 patch 是否覆盖了 tgkill 调用点

### Q3: forge_controller.py 认证失败

**错误**: `{"status":"err","msg":"auth failed"}`

**处理**:
```bash
# 清除本地 key 缓存，重新拉取
rm /tmp/.forge_key_cache
python forge_controller.py ping  # 会自动重新拉取
```

### Q4: BSS 扫描发现偏移数为 0

**原因**: libtersafe 版本更新，快照差分未发现变化（600ms 太短）

**处理**:
```bash
# 手动触发重新扫描
rm /data/local/tmp/forge_bss_map.json
adb shell su -c '/data/local/tmp/forge -m'  # 重新 patch + 扫描
```

### Q5: 游戏崩溃后 bot_runner 不自动恢复

**检查**:
```python
# 确保 set_forge_controller 被调用
bot.set_forge_controller(ctrl)

# 检查 cfg 中是否有 crash_backoff_max
cfg = {"crash_backoff_max": 300, "retry_limit": 5}
```

---

## 附录: v6.0 → v7.0 变更速查

```
libforgehook.c
  + g_hooks_ready 时序修复 (P0-1)
  + maps/smaps 动态缓冲 (P0-2)
  + constructor(47) dlopen 死锁修复 (P1-1)
  + 扩展过滤名单: libgadget/libzygisk/libsubstrate
  + 动态 PID: getpid()/gettid() 替代硬编码 12345 (P2-1)
  + tgkill: 拦截来自 libtersafe 段的所有信号 (Patch-A)
  + exit_group: g_hooks_ready=0 时也拦截 tersafe 调用 (Patch-B)
  + 新增 /proc 覆盖: smaps/wchan/syscall/fdinfo/SELinux attr

forge.c
  + SipHash-2-4 TCP 认证 (P2-2)
  + inject_hook: fork+execv 替代 system() (Fix)
  + daemon cycle 溢出防护: cycle %= 30 (Patch-C)
  + 版本号 6.0 → 7.0

runner/forge_controller.py
  + SipHash-2-4 认证集成
  + 自动拉取/缓存 session key

runner/bot_runner.py
  + 崩溃原因诊断 (tersafe_kill/native_crash/oom_kill)
  + forge 重打 patch 集成
  + 连续崩溃日志采集
  + 指数退避 (30s→300s)

cloud-agent/deploy.sh
  + libforgehook.so 编译加 -lpthread
```
