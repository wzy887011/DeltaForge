# DeltaForge v5.7 — 三角洲行动手游过检插件

> 最后更新: 2026-07-24  
> 仓库: github.com/wzy887011/DeltaForge  
> 分支: master  
> 最新 commit: `6306851` (待修复后再测)

---

## 一、项目是什么

**目标:** 在云手机上伪装成真机(Samsung Galaxy S10),绕过腾讯 TerSafe/ACE 反作弊系统的全部检测层,让三角洲行动(com.tencent.tmgp.dfm)不封号运行。

**核心原理:** 五层纵深防御对抗 TerSafe 的六层检测体系。

---

## 二、当前进度 — 做到哪了

| 模块 | 状态 | 说明 |
|------|------|------|
| 内存补丁 113 处 | ✅ | /proc/PID/mem 写入,TerSafe 64处 + BSS 40处 + UE4 6处 + kill链 3处 |
| 文件清理 | ✅ | 反作弊目录/文件定期清理,5秒循环守护 |
| 属性伪装 | ✅ | libforgehook JNI 覆写 Build.* / SystemProperties |
| Library hijack | ✅ | libforgehook.so 替换 libtdmqimei.so,绕过 linker namespace |
| seccomp-bpf 内核拦截 | ✅ | 拦截 tgkill(131)/tkill(130) 的 fatal 信号 |
| kill 链代码补丁 | ✅ | 6个节点全补,覆盖检测入口→调度→路由→封装→kill调用 |
| **游戏运行** | ❌ | **BPF死代码导致seccomp未安装, game dies** |

### 当前问题

**v5.6 root cause (2026-07-24 确认):**

1. **BPF 死代码被内核拒绝 → `Seccomp: 0`。** v5.6 的 BPF 中 tgkill 匹配后走 RET ALLOW,后面 tkill 和文件 syscall 的检查全在 RET 之后,是死代码。内核 BPF verifier 拒绝安装,确认 `cat /proc/PID/status | grep Seccomp` = `0`。

2. **`kill()` libc hook 太激进。** 拦截所有信号 1-31(包括 SIGCHLD/SIGCONT),可能破坏 Android runtime 进程组信令。

3. **`hook_log()` 写 app data 目录被 SELinux 阻止。** constructor 阶段 Java 未初始化,app data 目录无 SELinux 上下文,`openat()` 被拒。

**v5.7 修复 (本 commit):**
- BPF 重构:tgkill→tkill→file6→ALLOW 线性链,无死代码,全部 24 条指令可达
- `hook_log()` 改写到 `/data/local/tmp/forge_hook.log`
- `kill()` libc hook 改为只拦截 SIGKILL(9)/SIGTERM(15)

**当前等待:** 云机验证 v5.7。

---

## 三、五层防护 vs 六层检测

```
TerSafe 检测层                     DeltaForge 对抗层
─────────────────────────────────────────────────────────
L1 网络层(IP/ASN)          →     (暂无,iptables 仅清旧规则)
L2 系统层(属性/cpuinfo/GPU) →     libc hook 伪造 30+ 文件路径
L3 Java层(Build/Properties) →     JNI_OnLoad 覆写 + __system_property_get hook
L4 内核层(/proc/sys/virtio) →     35+ 路径隐藏 + seccomp TRAP
L5 运行时层(检测+kill)      →     113处代码补丁 + seccomp 拦截 kill syscall
L6 行为层(跑刀模式)          →     (bot_runner.py,P3 待实现)

我们的五层:
  L1 seccomp-bpf (constructor 49,最早)
  L2 maps 自隐藏 (constructor 50)
  L3 libc PLT hooks (so加载瞬间)
  L4 运行时代码补丁 (constructor 150,轮询等待 libtersafe.so)
  L5 外部 /proc/PID/mem 补丁 + 5秒回读
```

---

## 四、技术细节

### 4.1 PC → GitHub → 云手机 完整工作流

```
PC (Windows,本地编辑)
  │
  ├── git add / git commit / git push origin master
  │
  ▼
GitHub (wzy887011/DeltaForge)
  │
  ├── git pull origin master  ← 云手机 Termux
  │
  ▼
云手机 (Android, Termux + root)
  │
  ├── sh cloud-agent/deploy.sh
  │     ├── clang 编译 forge.c → forge
  │     ├── clang 编译 libforgehook.c → libforgehook.so
  │     ├── 编译 forge_monitor / injector / touch_injector
  │     ├── 生成 root 部署子脚本(heredoc)
  │     └── su -c 执行子脚本:
  │           ├── cp 到 /data/local/tmp/
  │           ├── find /data/app -name libtdmqimei_real.so
  │           └── 自动更新 hijack 位置的 libtdmqimei.so
  │
  ├── su -c '/data/local/tmp/forge -l'
  │     ├── do_prepare(): 属性伪装 → 文件清理 → 杀检测进程
  │     ├── start_game(): am start 启动游戏
  │     ├── inject_hook(): ptrace 注入 libforgehook.so(兜底)
  │     ├── patch_game_process(): /proc/PID/mem 写 113 处补丁
  │     └── 启动 5秒回读守护 + 文件清理守护
  │
  └── 崩溃后: su -c 'sh /data/local/tmp/check.sh'
```

### 4.2 Library Hijack (最关键的部署步骤)

Android 8+ linker namespace 隔离,从 `/data/local/tmp/` dlopen 会返回 NULL。hijack 方式解决:

```sh
# df-hijack-root.sh 做的事:
cd /data/app/.../com.tencent.tmgp.dfm.../lib/arm64/
mv libtdmqimei.so libtdmqimei_real.so    # 备份原版
cp libforgehook.so libtdmqimei.so        # 我们的 so 冒名顶替
```

游戏加载 `libtdmqimei.so` → 实际加载我们的 `libforgehook.so` → constructor 执行所有 hook 和补丁 → chainload 调用原版 `libtdmqimei_real.so`。

**deploy.sh 已自动处理:** 编译后自动 `find /data/app -name libtdmqimei_real.so` 找到 hijack 目录,用新版 so 覆盖。

### 4.3 云手机环境

| 项目 | 详情 |
|------|------|
| 型号伪装 | Samsung SM-G9730 (beyond1q) |
| Android 版本 | 11 (SDK 30) |
| Termux | 已安装,提供 clang 编译链 |
| Root | 已 root, su 可用(uid=0) |
| Magisk | 未安装(无 resetprop,属性伪装依赖 hook 兜底) |
| SELinux | 可能 permissive(云手机常见) |
| GPU | Mali (与伪装的 Adreno 740 矛盾 — 已知风险) |
| 编译器 | Termux clang, 参数: `-pie -Os -Wall`(不能用 `-static`) |
| 部署目录 | `/data/local/tmp/` (属主 root:root) |
| 游戏包名 | com.tencent.tmgp.dfm |

### 4.4 终端操作注意事项

1. **`su -c "..."` 长路径折行截断:** Android shell 的 `su -c` 遇到换行符会截断命令。长命令必须先写成脚本文件再 `su -c 'sh script.sh'`。

2. **`/data/local/tmp/` 权限:** 目录属主 root,Termux 普通用户(u0_a73)无法覆盖已有文件。deploy.sh 必须通过 heredoc 生成子脚本,再用 `su -c` 执行。

3. **`/data/app/` 不可读:** 普通用户无法访问,查找 hijack 路径必须在 root 下进行。

4. **Termux 编译限制:** `-static` 不可用(无静态 libc)。`resetprop` 不存在(Magisk 未装)。

5. **MD5 交叉验证是唯一可信的部署确认:** deploy.sh 输出本地和远端 MD5,必须一致。

### 4.5 文件结构

```
DeltaForge/
├── cloud-agent/
│   ├── native/
│   │   ├── forge.c              # 主控:属性伪装/文件清理/内存补丁/TCP server
│   │   ├── libforgehook.c       # 注入 hook 库:libc拦截/seccomp/JNI/代码补丁
│   │   ├── injector.c           # ptrace 注入器
│   │   ├── forge_monitor.c      # inotify 文件行为监控
│   │   ├── touch_injector.c     # /dev/uinput 触摸注入
│   │   └── Makefile             # NDK 交叉编译(PC端)
│   ├── deploy.sh                # 一键编译+部署
│   ├── check.sh                 # 诊断脚本(一次拉取所有状态)
│   ├── collect_logs.sh          # 崩溃后日志采集
│   ├── df-hijack-root.sh        # library hijack 安装
│   └── df-diagnose-root.sh      # root 侧诊断
└── runner/
    ├── bot_runner.py            # 自动跑刀引擎
    └── forge_controller.py      # forge TCP 控制客户端
```

---

## 五、诊断命令速查

```bash
# 部署(云手机 Termux)
cd ~/DeltaForge && git pull && sh cloud-agent/deploy.sh

# 清日志 + 启动游戏
su -c 'rm -f /data/data/com.tencent.tmgp.dfm/files/forge_hook.log'
su -c '/data/local/tmp/forge -l'

# 诊断(hijack版本/MD5/forge_hook.log/tombstone/进程maps)
su -c 'sh /data/local/tmp/check.sh'

# 崩溃后采集完整日志
su -c 'sh /data/local/tmp/collect_logs.sh'

# 手动验证 hijack so 版本
su -c "strings \$(find /data/app -name libtdmqimei_real.so | head -1 | xargs dirname)/libtdmqimei.so | grep 'v5\.'"
```

---

## 六、遗留风险

1. **GPU 指纹矛盾:** 云手机物理 Mali GPU,属性伪装 Adreno 740。`glGetString(GL_RENDERER)` 无法 hook,服务端可能检测到 GPU 不一致导致封号(非崩溃)。

2. **TerSafe 完整性校验:** 113 处补丁可能被 TerSafe 自身的 CRC 校验检测到。5秒回读+自动重打可以对抗被动还原,但无法对抗校验触发后的备用 kill 路径。

3. **`kill(129)` 无法在 seccomp 层拦截:** Android runtime 依赖 `kill()` 做进程组信令,拦截会导致瞬间崩溃。TerSafe 可能改用 `kill()` 绕过我们的 tgkill/tkill 拦截。

4. **服务端行为检测:** 跑刀模式(固定路线/停留时间)会被服务端识别为非人类行为。

---

## 七、v5.6-v5.7 session 改动记录

| commit | 内容 |
|--------|------|
| `e367fef` | v5.6 全面修复:seccomp 提前到 constructor(49),kill链补全6节点,tgkill/tkill拦截,BPF_JGE修复,audit缓冲 |
| `0e63142` | check.sh 诊断脚本 |
| `d9033e8` | 移除 seccomp kill(129) 无条件拦截(Android runtime需要);简化BPF逻辑 |
| `6306851` | 4个bug修复(tersafe async patch / injector hijack detection / restorecon / 移除 dead wrap.xxx) |
| (next) | **v5.7**: BPF 24指令线性链(无死代码→内核接受) + hook_log→/data/local/tmp/ + kill()只拦SIGKILL/SIGTERM |

**v5.6 已确认为 BPF 死代码 bug。** 指令[8]RET ALLOW 把后面整个 tkill 检查 + 文件 syscall 检查全变成死代码,内核 verifier 拒装 → `Seccomp: 0`。
