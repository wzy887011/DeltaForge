# DeltaForge v7.0 封号根因诊断+修复 — Claude Code 执行提示词

> 将此文件完整复制给 Claude Code。基于 github.com/wzy887011/DeltaForge (branch: master)

---

## 诊断结果

### 现象
游戏登录后 1-2 分钟被封，提示"使用第三方插件"或"环境异常"。

### 日志状态
```
forge_hook.log        → 文件不存在 (!!)
forge_audit.log       → 文件不存在 (!!)
forge_monitor.log     → 文件不存在
forge.log             → patch reverted 持续输出 (补丁在恢复)
logcat                → 无封号相关输出
```

### 根因分析

**hooks 从未激活** — 整个 libc 拦截层（文件伪装、属性hook、网络过滤、审计日志）全部无效。

原因链：
1. v7.0 将 `g_hooks_ready=1` 的时机从 constructor(150)末尾移到了 `_adjust_code_thread` 的 patch 成功后
2. 激活条件是 `ok >= KILL_CHAIN_N - 2` (即至少 4/6 节点 patch 成功)
3. 但 tersafe 持续恢复补丁（forge.log 满屏 "patch reverted"）
4. 后台守护虽然重打补丁，但 _adjust_code_thread 中的一次性激活检查只执行一次
5. 如果那一次检查时正好补丁被恢复(ok<4)，g_hooks_ready 永远不会设 1
6. 所有 libc hook 函数检查 `if(!g_hooks_ready) return _real_func(...)` — 全部透传
7. 游戏读取真实设备属性、连接上报服务器 → 封号

### 次要问题
- forge_hook.log 文件不存在：可能是 v7.0 的 de-meta-fy 重构改动了 constructor(48) 的日志写入
- forge_audit.log / forge_monitor.log 不存在：因为 g_hooks_ready=0，审计和监控功能未激活

---

## 修复方案

### Fix 1 [Critical] — hook 激活逻辑回退（对 inject 模式安全化）

**文件:** `cloud-agent/native/libforgehook.c`

**问题位置:** `_adjust_code_thread` 中 `g_hooks_ready` 设置逻辑

**修复:**
```c
// 在 _adjust_code_thread 中，找到 g_hooks_ready 设置代码
// 改为双路径激活:

// 路径A (原有): tersafe 补丁成功 → 激活 (hijack 兼容)
if (ok >= (int)KILL_CHAIN_N - 2) {
    __atomic_store_n(&g_hooks_ready, 1, __ATOMIC_RELEASE);
    hook_log("[hooks] v7.1 activated after patch success\n");
}

// 路径B (新增): inject 模式下 tersafe 已加载 → 立即激活
// 判断条件: tersafe base != 0 说明进程已完全初始化
if (!g_hooks_ready && base != 0) {
    __atomic_store_n(&g_hooks_ready, 1, __ATOMIC_RELEASE);
    hook_log("[hooks] v7.1 activated (inject mode, tersafe loaded)\n");
}

// 路径C (新增): 超时兜底 — 30秒后无论如何激活
static time_t g_thread_start = 0;
if (!g_thread_start) g_thread_start = time(NULL);
if (!g_hooks_ready && time(NULL) - g_thread_start > 30) {
    __atomic_store_n(&g_hooks_ready, 1, __ATOMIC_RELEASE);
    hook_log("[hooks] v7.1 activated (30s timeout fallback)\n");
}
```

### Fix 2 [Critical] — forge_hook.log 写入验证

**文件:** `cloud-agent/native/libforgehook.c` constructor(48)

**问题位置:** `_probe_loaded`

**诊断:** 在 constructor 入口增加双路径日志（同时写 /data/local/tmp/forge_hook.log 和 /sdcard/forge_diag.log），确认日志能否写入。

**修复:**
```c
__attribute__((constructor(48)))
static void _probe_loaded(void) {
    // 双路径写入确保至少一处成功
    const char *paths[] = {
        "/data/local/tmp/forge_hook.log",
        "/sdcard/forge_hook.log"  // fallback
    };
    for (int i = 0; i < 2; i++) {
        int fd = (int)syscall(SYS_openat, AT_FDCWD, paths[i],
            O_WRONLY | O_CREAT | O_APPEND, 0666);  // 注意: 0666 不是 0600
        if (fd >= 0) {
            const char *msg = "[CTOR] 48 probe v7.1\n";
            syscall(SYS_write, fd, msg, strlen(msg));
            syscall(SYS_close, fd);
        }
    }
}
```

**关键改动:** 文件权限从 `0600` 改为 `0666` — Android 上 app 进程写 /data/local/tmp/ 可能因权限问题失败。

### Fix 3 [Important] — 加大补丁守护检查频率

**文件:** `cloud-agent/native/forge.c`

**问题:** patch reverted 在持续发生，说明 200ms 间隔不足以压制 tersafe 的恢复。

**修复:**
```c
// 在 cleaner daemon 中:
// usleep(200000) → usleep(100000)  (100ms 间隔)
// kill chain 检查从不分周期（每周期都查）→ 每次迭代额外再查一次
```

### Fix 4 [Safety] — 封装检测定时器

**文件:** `cloud-agent/native/libforgehook.c`

**修复:** 在 `_adjust_code_thread` 补丁循环中，如果连续 3 次重试 ok 数仍 < 阈值，**不是不激活，而是立即激活** — 因为游戏已经登录了，保护比完美更重要。

```c
if (ok == 0) {
    consecutive_patch_failures++;
    if (consecutive_patch_failures >= 3) {
        __atomic_store_n(&g_hooks_ready, 1, __ATOMIC_RELEASE);
        hook_log("[hooks] v7.1 emergency activation (patch consistently failing)\n");
    }
}
```

---

## 执行要求

1. 按 Fix 1→2→3→4 顺序修改
2. 版本号改为 `"7.1"`
3. 文件头注释更新为 `v7.1`
4. 编译通过后 commit message: `fix: v7.1 hook activation reliability + dual-path logging`
5. push 到 origin master

---

## 验证方式

```bash
sh cloud-agent/deploy.sh --no-hijack
su -c '/data/local/tmp/forge -l'
sleep 15
su -c 'grep "v7.1 activated" /data/local/tmp/forge_hook.log'
# 必须看到激活日志
su -c 'grep "v7.1 activated" /sdcard/forge_hook.log'
# fallback 日志（如果主路径失败）
```
