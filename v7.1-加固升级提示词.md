# DeltaForge v7.1 安全加固升级 — Claude Code 执行提示词

> 将此文件内容完整复制给 Claude Code，它将自动完成全部升级。
> 基于当前仓库: github.com/wzy887011/DeltaForge (branch: master, commit: 14e00ed)

---

## 任务背景

当前 DeltaForge v7.0 是一套 Android 用户态过检测方案（ptrace注入 + 内存补丁 + libc hook），
需要升级到 v7.1，增加以下安全加固能力，缩小与内核级方案（如 CodeGuard）的差距。

所有改动基于用户态实现，不涉及内核模块。

---

## 升级清单（按优先级排列）

### P0 — 字符串常量加密（防 strings/IDA 检索）

**现状:** 敏感字符串明文出现在二进制中，`strings libforgehook.so | grep forgehook` 直接暴露注入库。

**要求:**
1. 在 libforgehook.c 和 forge.c 中新增 `CRYPT_STR()` 宏：
   ```c
   // 编译期 XOR 加密，运行时栈上解密后使用
   #define CRYPT_KEY 0x73
   #define CRYPT_STR(s) ({ \
       static const char _enc[] = { [XOR each char with 0x73] }; \
       char _dec[sizeof(_enc)]; \
       for(int i=0;i<sizeof(_enc);i++) _dec[i]=_enc[i]^CRYPT_KEY; \
       _dec[sizeof(_enc)-1]='\0'; \
       _dec; \
   })
   ```
   或等价实现（xorstr/xor_string 模式）。
2. 至少加密以下字符串：
   - `"libforgehook"`, `"libtdmqimei_real"`, `"libtdmqimei"`
   - `"/data/local/tmp/forge_hook.log"`, `"/data/local/tmp/forge.log"`
   - `"com.tencent.tmgp.dfm"` (如涉及)
   - `"libtersafe.so"`, `"libUE4.so"`
   - forge.c 中 `kHijackDirs` / `kTersafePatches` 等数组中的路径和模块名
3. 不对热路径（patch循环内的字符串）加密，保持性能。

### P1 — 每次启动随机化标识符

**现状:** 所有日志路径、memfd名、临时文件名固定，多次运行可被指纹识别。

**要求:**
1. 在 libforgehook.c 构造函数(48)中生成随机后缀（6位hex，来自 /dev/urandom 或 getpid^time 异或）
2. 应用到：
   - `forge_hook.log` → `forge_hook.<rand>.log`
   - memfd 名称 `"fh"`, `"ac"`, `"maps"` → `"fh_<rand>"` 等
   - `/dev/shm/.fh` → `/dev/shm/.fh_<rand>`
3. /data/local/tmp/ 路径保持不变（deploy.sh 依赖固定路径）

### P2 — 编译期随机垃圾指令注入

**现状:** 二进制结构固定，hash/特征码固定，可被 tersafe 特征库匹配。

**要求:**
1. 新增 `JUNK_INSN()` 宏，在关键函数中插入编译期随机 NOP 变体：
   ```c
   #define JUNK_INSN() __asm__ __volatile__("nop;mov %0,%0"::"r"(0):"memory")
   // 或等价效果: and x0,x0,#0; orr x0,x0,xzr 等零操作指令
   ```
2. 至少插入到以下函数：
   - libforgehook.c: `open()`, `fopen()`, `tgkill()`, `__system_property_get()`, `JNI_OnLoad()`
   - forge.c: `patch_insn()`, `safe_verify_and_write()`
3. 每个函数插入 2-4 条 JUNK_INSN，分布在不同位置

### P3 — mremap 进程内隐藏替代 madvise

**现状:** constructor(50) 用 `madvise(MADV_DONTDUMP)` 隐藏，但不防 /proc/self/maps 扫描。

**要求:**
1. 在 `_hide_self_from_maps()` 中实现 mremap 匿名重映射：
   ```
   for each memory segment of our so:
     1. mmap anonymous memory same size
     2. memcpy original → anonymous
     3. mremap(MREMAP_FIXED) replace original address
     4. mprotect restore permissions
   → /proc/self/maps 中文件路径替换为 [anon]
   ```
2. 同时保留现有 make_filtered_maps_fd() 作为双重保护
3. 仅在 inject 模式下执行（hijack 模式已弃用，不需要兼容）

### P4 — 属性查询流量混淆

**现状:** forge_audit 记录所有属性查询，但仅审计不混淆。游戏可能通过查询频率/顺序识别注入。

**要求:**
1. 在 __system_property_get hook 中，对非白名单属性查询返回缓存的真实值（延迟取一次）
2. 属性查询日志改为二进制格式（减少 I/O 开销）
3. 对于频繁查询的属性（同一秒内 >3次），随机返回抖动值（±1 或大小写变化）

### P5 — 无痕自身通信加固

**现状:** SipHash TCP 认证已实现，但命令名（"ping", "patch"等）在 TCP 流中明文可见。

**要求:**
1. TCP 命令使用单字节 opcode 替代字符串：
   - `ping`→0x01, `prepare`→0x02, `launch`→0x03, `patch`→0x04, `stop`→0x05, `status`→0x06, `clean`→0x07
2. 响应 JSON 字段名缩短（`status`→`s`, `version`→`v`），实际含义在注释中注明
3. 新增 session nonce 验证——连接建立后首字节必须是本次启动生成的随机 nonce

---

## 执行要求

1. **保持兼容性:** 所有改动不破坏现有 inject 模式部署流程（deploy.sh --no-hijack）
2. **版本号:** 统一使用 `"7.1"` 作为 FORGE_VERSION，文件头注释更新为 `v7.1`
3. **编译通过:** Termux clang, 参数 `-pie -Os -Wall`(forge) / `-shared -fPIC -Os -Wall -ldl`(libforgehook)
4. **分步提交:** P0/P1/P2/P3/P4/P5 各自独立 commit，避免巨型 diff
5. **不删除代码:** 被替换的旧实现用 `#if 0` 保留而非删除，方便回滚
6. **添加注释:** 每个加固点注释说明"防什么检测手段"

---

## 验证方式

1. `strings libforgehook.so | grep -i "forgehook\|qimei\|tersafe"` → 无明文输出
2. `su -c '/data/local/tmp/forge -l'` → 游戏能正常进入
3. `su -c 'cat /proc/<PID>/maps | grep forgehook'` → 行数减少或显示 [anon]
4. 换新号登录挂机 2 小时不封号
